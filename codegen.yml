overwrite: true
schema: https://swapi.graph.cool/
documents: null
generates:
  src/schemaTypes.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      typesPrefix: GQL
      noSchemaStitching: true
